'use strict'

module.exports = {
  ensureLoggedIn: (req, res, next) => {
    if (req.isAuthenticated()) {
      return next()
    }
    res.redirect('/login')
  },
  logOut: (req, res) => {
    // req.logout()
    req.session.destroy()
    res.redirect('/')
  }
}
