extends ../../../views/layout

block variables
  - var selected = "Unicorn"
  - var title = "Unicorns"

block content 
  h2 Create unicorn
  form.new.quiz(action="/unicorns/", method="POST")
    //- .label 
    //-   label(for="card-front") Card front
    //- .input.front 
    //-   input#card-front(type="text", name="card[front]", vaue="", autofocus)
    //- .label 
    //-   label(for="card-back") Card back
    //- .input.back 
    //-   textarea#card-back(name="card[back]")
    .label
      label Tag(s):
    .input.tags
      legend Choose tag
      select#tag-select(name="unicorn[tag]")
        each tag in tags
          option(value=tag.id)= tag.name
    .input.submit
    input(type="submit", value="Submit")