extends ../../../views/layout

block variables
  - var selected = "Card";
  - var title = "Cards";

block content
  h2 Cards 
  section.header.
    (#{cards.length} #{(cards.length !== 1) ? 'cards' : 'card'})
  section.cards.list.container
    each card in cards
      .item.columns
        .column.col-12
          .front
            strong 
              a(href=`/cards/${card.id}` title=card.front) #{card.front}
          .back= card.back
        .column.col-12 
          .tags-list
            each tag in card.tags
              span.chip.tag= tag.name
  section.footer
    if user
      button.new.btn.btn-primary.btn-block(hx-get='/cards/new', title='new card', hx-target='body') Create new card